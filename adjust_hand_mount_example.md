# 调整灵巧手装配位置

当前装配参数定义在 `sim/build_nero_inspire.py`，不要依赖旧文档中的行号：

```python
FLANGE_MOUNT_XYZ = "0 0 0.016489"
FLANGE_MOUNT_RPY = "0 0 1.570796"

MOUNT_XYZ = "0.000042 0. 0.002158"
MOUNT_RPY = "0 0 1.570796"
```

`MOUNT_*` 是适配法兰到手根的变换，XYZ 单位为米，RPY 单位为弧度。字符串中的
`0.` 与 `0.0` 数值等价。

## 调整方法

不要直接复制“向左/向前”的旧示例。平移分量是在父关节坐标系表达，经过法兰旋转后不
一定等于 world 方向。推荐流程：

1. 记录当前值和希望修正的实测偏差。
2. 在父坐标系中只修改一个平移轴，首次不超过 1 mm。
3. 重新生成装配并观察 mesh、关节轴和坐标系。
4. 平移确认后再单独调整旋转，首次不超过 1 度。
5. 用装配体/测量数据验证，不以单一视角“看起来对齐”为依据。

例如仅将父坐标系 Y 增加 1 mm：

```python
MOUNT_XYZ = "0.000042 0.001 0.002158"
```

例如仅将 yaw 增加 1 度：

```python
MOUNT_RPY = "0 0 1.588249"
```

## 重新生成与验证

```bash
python3 sim/build_nero_inspire.py
python3 sim/build_combo_viz.py
```

然后在已启动的 Web 工作台中刷新合体模型。不要用 `pkill -f` 作为文档默认操作；先在
运行服务的终端正常停止，避免误杀其他开发进程。

验证至少包括：

- 生成脚本的 URDF/MuJoCo 加载检查通过
- 法兰和手根无明显穿插或悬空
- 手指关节轴与 mesh 方向一致
- q=0 坐标系符合 [HARDWARE.md](HARDWARE.md) 的约定
- 真机安装前由机械尺寸或装配 CAD 复核

大幅旋转或平移会同时改变碰撞、工具中心点和 VLA/retargeting 映射，不应只更新可视化。

---

**最后核对**：2026-08-14
