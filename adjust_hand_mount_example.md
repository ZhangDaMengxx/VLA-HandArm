# 调整灵巧手装配位置示例

## 编辑 sim/build_nero_inspire.py

找到第53-54行：

```python
# 原值（从装配体反解，ICP残差0.36mm）
MOUNT_XYZ = "0.000042 0.005962 0.002158"
MOUNT_RPY = "0 0 1.570796"
```

### 调整示例

#### 例子1：手往前（沿Z轴）移动5mm
```python
MOUNT_XYZ = "0.000042 0.005962 0.007158"  # Z: 0.002158 + 0.005
MOUNT_RPY = "0 0 1.570796"
```

#### 例子2：手往左（沿Y轴）移动3mm
```python
MOUNT_XYZ = "0.000042 0.008962 0.002158"  # Y: 0.005962 + 0.003
MOUNT_RPY = "0 0 1.570796"
```

#### 例子3：手绕Z轴旋转额外15°
```python
MOUNT_XYZ = "0.000042 0.005962 0.002158"
MOUNT_RPY = "0 0 1.832596"  # 1.570796 + 0.261799 (π/2 + π/12)
```

#### 例子4：改变手的朝向（绕X轴翻转）
```python
MOUNT_XYZ = "0.000042 0.005962 0.002158"
MOUNT_RPY = "3.14159 0 1.570796"  # Roll=π (180°翻转)
```

## 重新生成装配URDF

```bash
# 1. 编辑完参数后，重新运行脚本
python3 sim/build_nero_inspire.py

# 2. 重新生成浏览器viz版本
python3 sim/build_combo_viz.py

# 3. 重启web服务器查看效果
pkill -f app_web.py
~/gradio_venv/bin/python sim/app_web.py
```

## 坐标系说明

**Link8坐标系**（法兰父坐标系）：
- X: 向前（机械臂工作空间内侧）
- Y: 向左
- Z: 向上（沿腕部旋转轴）

**调整时的方向**（q=0臂竖直状态）：
- +X: 往机械臂内侧
- +Y: 往左
- +Z: 往上（远离基座）

## 验证方法

1. **MuJoCo验证**：`build_nero_inspire.py`运行完会自动加载验证
2. **Web 3D可视化**：浏览器中查看combo页面
3. **实际抓取测试**：真机测试手部是否能正确对准目标

## ⚠️ 注意事项

1. **单位是米**：0.001 = 1mm
2. **角度用弧度**：π ≈ 3.14159，π/2 ≈ 1.5708
3. **原值来自装配体反解**：ICP残差0.36mm，已经相当精确
4. **大改需要验证**：改动超过5mm建议用MeshLab/Blender对比装配体验证
